name:                ninety-nine
version:             0.1.0.0
github:              "Spaceface16158/Ninety-Nine-Haskell-Problems"
license:             BSD3
author:              "Amrit Rathie"
maintainer:          "amrit.rathie@utdallas.edu"
copyright:           "2022 Amrit Rathie"

extra-source-files:
- README.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/Spaceface16158/ninety-nine#readme>

dependencies:
- base >= 4.7 && < 5
- HUnit

library:
  source-dirs: src

#executables:
#  ninety-nine-exe:
#    main:                Main.hs
#    source-dirs:         app
#    ghc-options:
#    - -threaded
#    - -rtsopts
#    - -with-rtsopts=-N
#    dependencies:
#    - ninety-nine

tests:
  ninety-nine-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - ninety-nine
    - HUnit
